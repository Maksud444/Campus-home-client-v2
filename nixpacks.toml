[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build']
cacheDirectories = ['.next/cache', 'node_modules/.cache']

[start]
cmd = 'npm run start'
